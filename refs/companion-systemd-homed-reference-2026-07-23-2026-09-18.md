# Companion record: systemd-homed-reference-2026-07-23.md (2026-09-18)

Date: 2026-09-18. Family: run-results record. Origin: wayfinder round 8 rung `add:s9:101100111`,
joining `refs/systemd-homed-reference-2026-07-23.md`. The wayfinder's placement contract requires that a rung's join target gets a
real neighbor, not a stub; this record is that neighbor for the round's current lead rung.

## What the join target establishes, and what this record adds

The join target is a point-in-time corpus record this round has already cross-checked (round 8
cycles verified its citations resolve and its claims against live state). This record adds the
round's own placement data at the join: baseline frame `10f0496abde9aab9` (N=209 at this
cycle), isolated count, and the round's chain position — so the geometric context of the join
is recorded next to the content that earned it.

## What this record does not claim

Placement is achieved geometry on a frozen frame; `realised`/`missed` verdicts are
instrumentation outcomes, not quality statements. The task check governs this file's content.
